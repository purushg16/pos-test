import { InternalConfig, Resolver, ResolverMap } from './types';
export declare const DEFAULT_PREFIX = "";
export declare const DEFAULT_SUFFIX = "";
export declare const DEFAULT_MAX_DELAY = 20;
export declare const configResolver: {
    [K in keyof InternalConfig]: Resolver;
};
export declare function resolveWith<T extends {
    [k: string]: any;
}, V extends {
    [k: string]: any;
}>(config: Partial<T> | undefined, resolvers: ResolverMap): V;
